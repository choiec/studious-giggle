# Documentation Rules

- Put user-facing setup and workflow guidance in the closest entry point, usually the README or feature doc.
- Keep commands copy-pasteable and include required environment variables or prerequisites.
- Distinguish shipped behavior from planned follow-up work.
- When behavior is unverified, say so instead of implying it was tested.
- Prefer short examples that match the actual commands and file paths used in the repository.