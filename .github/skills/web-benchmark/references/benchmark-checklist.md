# Benchmark Checklist

- Does the page load successfully with no blocking console errors?
- Are there failed API or asset requests during first render?
- Is the main task usable before secondary widgets finish loading?
- Are images, scripts, or API calls obviously heavier than expected?
- If a regression is suspected, what was the previous baseline or expectation?
- What threshold would count as unacceptable for this route?